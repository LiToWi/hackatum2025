// Convex schema removed — kept placeholder to avoid build errors in projects
// This file intentionally contains no table definitions. If you need to re-add
// Convex schema later, recreate the expected exports here.

export default {}
